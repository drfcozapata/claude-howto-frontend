// Tailwind CSS v4 - Configuration is now optional
// Most configuration can be done in CSS, but we keep this for compatibility

export default {
  content: ['./index.html', './src/**/*.{vue,js,ts,jsx,tsx}'],
  theme: {
    extend: {},
  },
}
